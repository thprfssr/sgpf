#ifndef MAIN_H
#define MAIN_H

#include <stdint.h>
#include <inttypes.h>

#define MAX_INTERVAL_BYTE_SIZE		2000000000

const uint64_t MAX_INTERVAL_SIZE = MAX_INTERVAL_BYTE_SIZE / sizeof(uint64_t);


#endif
